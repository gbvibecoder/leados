// Prisma client placeholder
// Prisma 7+ requires an adapter. API routes use mock data for development.
// When deploying with a real DB, configure the adapter here.

export const prisma: any = null;
